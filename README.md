# go-npm with goreleaser


## build

```code
goreleaser release --rm-dist
```
## Publish 

```code
yarn p
```
## install

```code
npm install -g @dalongrong/go-npm-learning  --registry=https://registry.npmjs.com
```